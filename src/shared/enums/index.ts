export const enum EFetchState {
  LOADING = "loading",
  IDLE = "idle",
  ERROR = "error",
}
export const enum EPreviewType {
  NORMAL,
  VERTICAL,
  TOP10,
}
export const enum EMediaType {
  MOVIE = "movie",
  SERIE = "serie",
}
